declare module '@iconscout/react-unicons' {
    export interface IconProps {
      // Define las propiedades de los iconos
      name: string;
      size?: string;
      color?: string;
      // Agrega otras propiedades según sea necesario
    }
  
    // Define cualquier otro tipo que necesites
  
    // Exporta los componentes de iconos
    export const UilTimes: React.FC<IconProps>;
    // Exporta otros componentes de iconos según sea necesario
  
    // Exporta cualquier otra cosa que necesites
  }
  